"""Strategy definitions for the research plane — the metadata layer over the
execution registry's opaque `compute()` strategies. See `spec` for the primitive
taxonomy tags and the bounded parameter search space.
"""
