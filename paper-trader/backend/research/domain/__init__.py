"""Research-plane domain model — persisted to research.db on a dedicated
declarative base (`research.domain.base.ResearchBase`), kept strictly separate
from the execution ledger's `app.db.models.Base`.
"""
