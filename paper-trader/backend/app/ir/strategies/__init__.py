"""Real strategies expressed in the Component IR.

Nothing here executes in production. These modules exist so that the IR is
tested against the thing the platform actually trades rather than against
artefacts written to fit it — RFC 0001 Appendix A.1 is a sketch of
`expanding_z_v4`; `expanding_z.py` is the runnable article.
"""
