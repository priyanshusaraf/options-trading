Reference: [section index](../STRATEGY_OS_V0_V6_OWNER_DIRECTION_RECONCILIATION_2026-08-29.md). Read with its scope; this is not a new assignment.

## Current V0 programme reconciliation

Keep the accepted launch-convergence queue. Do not duplicate its work.

### Already covered by an exact accepted queue owner

- monitoring-only Type 1 target and SL/TP language contract;
- data-only provider credential boundary;
- provider capability evidence and typed historical refusal;
- canonical Kite observation and prospective capture path;
- monitoring persistence/runtime/API and reserved frontend monitoring/alerts path;
- trusted plan/catalogue, Razorpay webhook, coupons/trials and entitlements;
- privacy-safe analytics, structured support and platform-operator admin;
- five-family catalogue convergence;
- desktop builder, research, connections, billing/account and admin modules;
- security, operations, deployability, golden benchmarks and final V0 review.

`Covered` means routed, not implemented or accepted.

### Exact missing or partial obligations

1. A durable canonical monitoring event distinct from the legacy execution
   `SignalEvent`.
2. A durable `SignalAlert` with exact strategy/monitor/instrument/time/freshness/
   reason identity and one idempotent derivation from a monitoring event.
3. `AlertDeliveryAttempt` facts and separate append-only attention events with a
   rebuildable projection. Signal validity/expiry is not read state.
4. Alert filters, unread counts, detail evidence and attention mutations in the
   already-reserved monitoring API/frontend owners.
5. Google identity verification, stable subject linking and account-link policy.
6. Account export/deletion/retention and related privileged-action audit closure.
7. A distinct onboarding/home owner for the starter strategy through first alert
   journey.

### Collision-safe amendments

Use this order. Migration numbers are reserved names from the accepted queue and
must be rechecked against the actual head at dispatch.

```text
active monitoring-intent contract closes
        ↓
new strategy-os-v0-signal-alert-attention-contract
    pure contracts/tests only; no schema/routes/registry
        ↓
amended strategy-os-v0-monitoring-persistence
    sole shared DB owner; planned 0044
    MonitoringAssignment
    MonitoringSignalEvent (distinct from legacy execution SignalEvent)
    SignalAlert
    AlertDeliveryAttempt
    AlertAttentionEvent + rebuildable projection
        ↓
amended monitoring runtime
    event then alert, idempotently; delivery failure loses neither
        ↓
amended monitoring API
    list/filter/detail/unread/cursor + read/acknowledge/dismiss
        ↓
reserved desktop monitoring/alerts owner
```

Serialize the remaining shared model/migration work:

```text
monitoring/alerts persistence (planned 0044)
→ platform operations/billing persistence (planned 0045)
→ Google identity/session linking (next free head; candidate 0046)
→ prospective derivatives capture (next free head; old 0046 reservation shifts)
```

Do not renumber or edit a migration until the dispatch owner rechecks actual heads,
upgrade starts, role/copy/restore contracts and downgrade/forward-repair rules.

Additional owners:

- `strategy-os-v0-account-lifecycle-privacy` after Google identity, platform
  operations and structured support;
- `strategy-os-v0-onboarding-starter-journey` after the production frontend
  foundation, builder/research, Google identity, monitoring/alerts and billing
  account paths are accepted.

The full code/queue collision receipt is:

```text
.agent/runs/strategy-os-v0-v6-handoff-reconciliation/agents/v0-gap-collision/report.md
```

## Future architecture reconciliation

Existing ADRs and implementation foundations already preserve most necessary V1
and V4 seams:

- one execution binding;
- provider-neutral `ExecutionProductPolicy` before sizing;
- fixed-point sizing and target-position request;
- pending-order-aware execution delta;
- deterministic closed-batch admission and transactional reservations;
- atomic groups;
- single-instrument campaign/tranche/fill allocation and exact ownership;
- immutable static scope and canonical instrument identity.

Do not reinterpret `PositionCampaignRecord`. The narrow next architecture slice is
a documentation-only non-implementation ADR that:

1. freezes ADR 0018 campaign meaning;
2. reserves additive `EconomicPosition` for V4 multi-leg aggregation;
3. defines one strategy result to zero-or-many economic-intent correlation without
   creating an idle table;
4. reserves `ProposedPortfolioRevision` for V2 and states it is never a
   `PortfolioAdmissionDecision`, deployment, reservation or order authority;
5. records the V3 asset/managed split and V5/V6 `EconomicExposure` boundary.

No future noun, solver, service, marketplace schema or treasury integration is
needed in V0 or V1.

Focused evidence for the existing execution foundations: 96 passed and one
PostgreSQL-dependent test skipped. This is local contract evidence only, not a
behavior switch, PostgreSQL concurrency, provider, live or deployability proof.

Full receipt:

```text
.agent/runs/strategy-os-v0-v6-handoff-reconciliation/agents/future-seams/report.md
```

## Scale and professional-engineering reconciliation

Keep one professional-engineering programme, one source registry, one claim
registry and one corpus lineage.

The new scale/reference documents add:

- research-heavy, monitoring-heavy and future execution-heavy workload profiles;
- non-authoritative 0–50, 50–500, 500–2,000, 2,000–5,000 and 5,000–10,000 planning
  envelopes;
- per-user/job/monitor/deployment cost and provider-rights drivers;
- team/funding/operational-ownership triggers;
- a measurable architecture-adoption registry;
- V0 Alerts/Auth/Razorpay/Admin and V2–V6 application questions.

They do not authorize a second corpus crawl, duplicate manifests, microservices,
Kafka, Kubernetes, Redis, a workflow engine, a new database, event sourcing,
CRDTs, multi-region, HFT infrastructure or another runtime.

Use Stage A, approximately 0–50 serious users, only as a planning envelope. Current
safe work is measurement, quotas, recovery and release-deployable evidence. No
capacity, production or bottleneck claim was established.

Do not start the V4 corpus run as complete until the missing V3/simplicity inputs
are recovered or the owner explicitly accepts V2 plus the reconciled V4 delta as
the controlling composite. Continue the existing inventory; do not recrawl it by
default.

Full receipt:

```text
.agent/runs/strategy-os-v0-v6-handoff-reconciliation/agents/scale-reference/report.md
```

## Running-task and collision decision

The following already-active work may finish unchanged:

- corrected session-prefix independent assurance;
- Q02 data-only connection correction and single recheck;
- Q14 local release-operations correction and single recheck;
- Precision Slate production-convergence correction/recheck;
- unpublished monitoring-intent Type 1 contract.

Do not materialize monitoring persistence, platform-operations persistence,
Razorpay, analytics/support/admin, Google identity, prospective-capture migrations
or later frontend/domain queue capsules until the programme owner integrates this
reconciliation and seals the revised queue.

`CURRENT.md` records this dedicated reconciliation and the pause. The active
machine stage remains the unrelated indicator assurance. `PROGRAMME.json` does
not yet contain a primary reconciliation entry. The programme owner should record
this as a completed side assignment or accepted reconciliation without replacing
the active numerical stage.
