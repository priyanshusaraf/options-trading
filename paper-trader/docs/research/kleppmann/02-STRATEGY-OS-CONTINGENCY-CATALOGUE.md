# First-packet contingency catalogue

Status: six assessed scenarios; the mandate's full catalogue remains open. Unknown prevention or recovery is not treated as absent or correct.

| ID / domain | Scenario and trigger | Assumption / user consequence | Current prevention and evidence | Detection / recovery | Severity / likelihood | Invariant, test and disposition |
| --- | --- | --- | --- | --- | --- | --- |
| JOB-01 / jobs | Worker resumes after its lease expires and another worker claims the run. | Expiry does not kill the old process. A stale worker could publish misleading terminal evidence. | `_active_claim`, `append_claimed_result_batch`, `complete_claim`; existing replacement test; independent actual-database probe. | Rejected write returns false; replacement remains current. Whole-process recovery not proven here. | Critical research consequence if violated; likelihood not estimated. | Only current owner/token can write. Local probe plus killed token mutant; KEEP current writer design. |
| JOB-02 / concurrency | Two sessions claim the same pending run at the same instant. | A sequential unit test does not exercise contention. Duplicate workers could compete for cost/result ownership. | `claim_run` conditional update; independent two-thread barrier probe. | Exactly one current token; loser receives no claim. Retry scheduling outside the probe remains unassessed. | Important/Critical depending result sink; likelihood not estimated. | One current authoritative claimant. Preserve transactional predicate; no Redis adoption. |
| JOB-03 / tenancy | Another owner presents the current token for a guessed run ID. | Token possession alone must not grant another tenant's write access. | Owner predicate in active-claim update; foreign-owner heartbeat in independent probe. | Refusal; no change to current claim. Broader auth/socket/storage audit remains open. | Critical if violated; likelihood not estimated. | Owner and run identity bind the claim. Retain owner-scoped predicate. |
| TIME-01 / observability | NTP steps the wall clock during a dataset read. | Time-of-day is assumed monotonic. `read_seconds` can become negative or inflated and mislead capacity evidence. | The runner already uses monotonic time for readiness, but dataset-store read timing uses `datetime.now().timestamp()`. See KCA-001. | Metric inspection can reveal a negative value; no automatic correction is defined. Dataset bytes and identity are unaffected. | Routine operational consequence; plausible on a corrected host clock. | Single-process elapsed measurements use a monotonic clock. Future fake-clock regression; no migration; research-spine owner. |
| EVT-01 / derived delivery | Domain fact commits, wake-up is lost, or dispatcher crashes after an effect before acknowledgement. | Notification is mistaken for authority or effects are assumed exactly once. | Transaction-bound outbox, polling and idempotent producer identity; existing crash test expects at-least-once replay. See KCA-002. | Poll durable outbox and reapply idempotently. Future monitoring consumers remain unproven. | Critical if authority diverges; likelihood not estimated. | Durable source fact survives notification loss. Keep current outbox; no event-platform adoption. |
| GRAPH-01 / editor conflict | Two editors save executable changes from the same base revision. | Last-write-wins or structural convergence is assumed semantically safe. A user edit can disappear or produce an unreviewed strategy. | `save_draft` checks `base_revision` before and in the atomic update; published graphs are immutable; layout is separate. See KCA-004. | Raise `GraphConflict`; user must reload/reconcile. Future collaboration UX is unassessed. | Important now, Critical before shared publication; likelihood not estimated. | Stale executable edits refuse. Preserve optimistic concurrency and reject V0 CRDT adoption. |

Sources and exact code pointers: [job packet](source-notes/job-claim-fencing.md). Migration: none. Rollback: remove only new evidence artifacts if desired; no product change to roll back. Follow-on owner: the next bounded research-durability review, before the `strategy-os-v0-canonical-research-spine` and operations release gates close.

The complete checklist still includes order timeouts, capital contention, partial
fills, held contracts, provider change, market-clock jumps, semantic caches, OOS,
schema rollback, restore, deletion, credentials, backpressure and uncertainty UX.
They are requirements in the preserved mandate, not assessed findings in this
table.
