# Implementation candidate gate

## Gate result

Five direct defects reproduce, but only four are current V0 implementation
candidates. None is authorized for mutation in this Chat 2 task yet because:

- `CURRENT.md` still points to the canonical-research-spine capsule whose product
  path ownership was released to frontend recovery;
- the active V0 convergence task is concurrently changing shared backend,
  programme and frontend state;
- the capsule protects several required inputs and does not list these corrections;
- schema/migration, execution and future-provider work require separate gates.

The user’s Chat 2 request authorizes this research packet and bounded fixes after
the repository gates pass. It does not erase collision ownership or create a safe
product write boundary.

## Candidate evaluation

### C1 — OOS selection isolation

| Question | Answer |
| --- | --- |
| Exact reachable failure | Qualification consumes all 400 rows later used for OOS validation. |
| Repository evidence | Current-tree RED in `data-research-characterizations-current-tree.log`. |
| Source principle | DSR/PBO require complete trial population and selection-free OOS. |
| Canonical requirement | V0 promises locked OOS and reproducible evidence. |
| Current-stack solution | Yes: split/seal boundaries or remove confirmatory label. |
| Smallest change | Introduce an explicit selection/qualification cutoff before calling qualification and persist the method/boundaries. |
| Compatibility | Old runs remain exploratory/contaminated; no relabel. |
| Migration/backfill | Prefer new method/spec identity; do not backfill false OOS status. |
| Rollback | Stop new confirmatory runs and retain old/new evidence. |
| RED/GREEN | OOS suffix mutation must not alter qualification/search/selection. |
| Complexity | Small code path plus evidence identity; no component. |
| Release | `V0 BLOCKER`. |
| Gate | `APPROVE FOR A NEW RESEARCH-VALIDITY CORRECTION CAPSULE`; not writable now. |

### C2 — V0 legacy sweep refusal

| Question | Answer |
| --- | --- |
| Exact reachable failure | V0 route policy permits `POST /api/backtest/sweep`. |
| Repository evidence | Current-tree policy RED. |
| Source principle | Historical research needs explicit point-in-time dataset/instrument/rulebook authority. |
| Canonical requirement | V0 canonical-manifest-only research surface. |
| Current-stack solution | Yes: release-profile/shared route denial before dispatch. |
| Smallest change | Deny both API prefixes under V0; preserve standard profile. |
| Migration/backfill | None. Existing legacy results keep their lower authority. |
| Rollback | Restore standard-profile compatibility only; never silently expose in V0. |
| RED/GREEN | Provider, universe, queue and repository spies remain untouched after refusal. |
| Complexity | One existing policy seam; no component. |
| Release | `V0 BLOCKER`. |
| Gate | `APPROVE FOR A NEW RELEASE-PROFILE CORRECTION CAPSULE`; not writable now. |

### C3 — ResourcePlan bridge

| Question | Answer |
| --- | --- |
| Exact reachable failure | Published graph research reaches `run_experiment` with no plan input/evidence. |
| Repository evidence | Current-tree structural RED. |
| Source principle | Admit bounded demand before work; unknown is not zero/unlimited. |
| Canonical requirement | Existing F03 and one canonical `ResourcePlan`. |
| Current-stack solution | Yes: reconstruct/verify/bind existing plan. |
| Smallest change | Add the accepted plan/address at the existing graph-to-research bridge and result identity. |
| Migration/backfill | Additive only if persistence lacks address; old evidence remains limited. |
| Rollback | Stop plan-bound new writes, keep old records and refusal semantics. |
| RED/GREEN | Every plan dimension/mismatch/refusal/restart mutation. |
| Complexity | Wiring/evidence only; no scheduler or second planner. |
| Release | `V0 BLOCKER`, existing F03. |
| Gate | Protected accepted inputs and named owner require a separate correction lineage. |

### C4 — collision-safe experiment reuse

| Question | Answer |
| --- | --- |
| Exact reachable failure | Different recipe bytes forced to one ID reuse one immutable row. |
| Repository evidence | Current-tree forced-collision RED. |
| Source principle | Idempotency key plus parameter equality; stable canonical bytes before hashing. |
| Canonical requirement | Immutable evidence identity must reconstruct exact inputs. |
| Current-stack solution | Yes: stored recipe byte comparison and mismatch refusal. |
| Smallest change | Add equality guard before any address-width migration. |
| Migration/backfill | Equality guard needs none; full 256-bit address would require expand-contract. |
| Rollback | Remove new-address writes but retain aliases/rows; never merge distinct bytes. |
| RED/GREEN | Forced collision, restart, legacy lookup, guard mutation, SQLite/PostgreSQL. |
| Complexity | Low for guard; migration risk conditional. |
| Release | `V0 NON-BLOCKING HARDENING`. |
| Gate | New identity/persistence capsule; use database-migration-safety if address changes. |

### C5 — terminal execution conflict

| Question | Answer |
| --- | --- |
| Exact reachable failure | Cancel then complete yields CANCELLED with full fill. |
| Release | `V1 EXECUTION`, V0 denied. |
| Decision | `DEFER`. Preserve RED test and raw-event requirement; do not implement future execution in Chat 2. |

## Candidates rejected from implementation

- Generic bitemporal database/table rewrite.
- Workflow engine, queue, Redis, Kafka, new ledger or service extraction.
- Provider-capture timestamp guard before producer/forming-state inventory.
- Statistical method replacement before dependent-trade comparisons.
- Claim-race code change before the timeout is localized.
- Socket revocation design before owner latency policy.
- QoS scheduler before a measured overload failure.

## Required next authority

The V0 programme owner should serialize four correction capsules, with C1–C3 ahead
of C4 unless current path ownership dictates a safer order. Each capsule must name
allowed/protected paths, inherited hashes, stopping condition, risk tier, exact RED
tests and reviewer gate. Until then, Chat 2 continues with architecture, scale and
internal-documentation deliverables only.
