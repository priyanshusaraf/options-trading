---
{
  "id": "phase1-4-foundation-postgresql16-semantic-package-contract",
  "phase": "interphase-4-5",
  "status": "blocked",
  "goal": "Materialize the closed recursive semantic-package wire, low-level PQ, total parser, provenance, protocol-fixture, digest, independent-consumer, and exhaustive mutation contracts without opening PostgreSQL.",
  "goal_contract": {
    "create_before_work": true,
    "durable_goal_count": 1,
    "stopping_condition": "Complete only when one concrete SYNTHETIC_PROTOCOL_ONLY package passes a separately implemented exhaustive consumer, every schema-derived leaf and binding mutation rejects, the protocol type cannot carry or promote semantic truth or root acceptance, and report, manifest, scope, architecture, and final same-byte hashes pass. Otherwise return BLOCKED."
  },
  "dependency_gate": "BLOCKED_UNTIL_ROOT_ACCEPTS_THE_EXACT_COMPLETED_TRIGGER_FIXTURE_CONTRACT_REPORT_MANIFEST_OUTPUTS_AND_CAPSULE_HASH",
  "dispatch_contract": "Root dispatch binds this capsule SHA-256, accepted recovery and trigger-fixture report/manifest/output hashes, both predecessor acceptance artifact hashes, HEAD, CURRENT, PROGRAMME, frozen source and protected hashes.",
  "root_acceptance_gate": {
    "path": ".agent/runs/phase1-4-foundation-critical-closure/root/postgresql16-semantic-package-contract-acceptance.json",
    "ownership": "root-only outside successor allowed_paths",
    "hash_authority": "exact SHA-256 supplied by the subsequent root dispatch and consumed read-only"
  },
  "ownership_contract": {
    "owns": "generic low-level PQ wire, parser, digest, provenance, and protocol-package contracts",
    "consumes_read_only": "root-accepted exact source-derived fixture and command values",
    "forbids": "changing source-derived fixture or command values and carrying semantic truth or root acceptance"
  },
  "required_docs": [],
  "risk_tags": ["critical", "postgresql", "semantic-package", "independent-consumer", "research-integrity", "false-green-evidence"],
  "allowed_paths": [
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract"
  ],
  "read_only_paths": [
    "paper-trader/backend", "paper-trader/frontend", "paper-trader/docs/agent", ".agent/review-package.json",
    ".agent/runs/phase1-4-foundation-critical-closure/root/postgresql16-semantic-package-contract-acceptance.json",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-authority-cleanup-recovery",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-authority-fixture-consumer-repeated-failure-recovery",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-trigger-fixture-contract"
  ],
  "required_outputs": [
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/semantic-package-contract.json",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/protocol-fixture-package.json",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/independent_consumer.py",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/mutation_driver.py",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/negative-mutation-registry.json",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/database-free-validation.log",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/report.md",
    ".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract/evidence-manifest.json"
  ],
  "execution_boundary": {
    "initial_attempts": 1,
    "bounded_repairs": 1,
    "repair_rule": "At most one repair may address the same material defect in the same bytes. A repeated or different material defect returns BLOCKED to root.",
    "parallel_budget": 0,
    "children": 0,
    "postgresql_allowed": false
  },
  "tests": [
    "Freeze and independently encode every primitive, enum, optional, fixed tuple, ordered array, closed record, digest reference, and external-root-acceptance value with declared recursive types.",
    "Consume every low-level PQ command/value/result/error field, total parser rule, provenance leaf, fixture/state/cleanup/package binding, digest edge, ordering edge, artifact hash, and scope claim.",
    "Derive every concrete mutation from closed schemas and named bindings, execute them one at a time, prove rejection codes and exact baseline restoration, and reject hand-entered count thresholds.",
    "Prove ProtocolFixturePackage/v1 cannot deserialize as Postgresql16IndependentSemanticAuthorityPackage, has no semantic truth or root-acceptance field, and cannot be promoted.",
    "Run scope, architecture, git diff --check, frozen/protected hashes, manifest attestation, and final same-byte validation."
  ],
  "test_plan": [
    "Materialize the accepted generic PQ, recursive wire, parser, digest, provenance, and package contracts into ignored evidence while consuming predecessor fixture and command values read-only.",
    "Run the separate independent consumer and mutation driver over the complete synthetic package, every concrete schema leaf and named binding, actual rejection identities, and exact baseline restoration.",
    "Seal report and manifest only after scope, repository architecture, frozen/protected hash, and final same-byte checks pass; then stop for root acceptance."
  ],
  "acceptance": [
    "The independent consumer imports neither producer nor canonical encoder and validates the complete concrete package.",
    "Every leaf and cross-record binding has one exact derived mutation receipt and exact restoration proof.",
    "Root accepts the report, manifest, outputs, logs, source/protected hashes, and this capsule's completed bytes before evidence work can be dispatched."
  ],
  "owner_gates": [
    "Stop on any predecessor, source, protected, CURRENT, PROGRAMME, HEAD, dispatch, or acceptance hash mismatch.",
    "Stop if any packed field lacks a total parser, any PQ element is unconsumed, any provenance wildcard remains, or protocol and semantic package types can be confused.",
    "Return to root for exact same-byte acceptance; do not activate the complete-evidence capsule."
  ],
  "stop_conditions": [
    "Any PostgreSQL/server/database, product, test, migration, runtime, dependency, provider, frontend, deployment, production, live, order, or money action is proposed.",
    "The consumer imports producer/encoder code, trusts self-digests, uses runtime types, generic item/field tags, partial parsers, synthetic semantic claims, or a hand-entered mutation minimum.",
    "A second material repair would be needed."
  ],
  "model_route": {
    "owner": "gpt-5.6-sol",
    "owner_reasoning_effort": "medium",
    "service_tier": "priority",
    "fresh_owner_required": true,
    "assignment_id": "foundation_postgresql16_semantic_package_contract_owner"
  },
  "parallel_budget": 0,
  "assignments": [],
  "review": {
    "required": false,
    "assignment_id": "foundation_postgresql16_semantic_package_contract_owner",
    "base_sha": "HEAD",
    "review_paths": [".agent/runs/phase1-4-foundation-postgresql16-semantic-package-contract"],
    "exclude_paths": ["paper-trader/backend", "paper-trader/frontend", "paper-trader/docs/agent", ".agent/review-package.json"],
    "verdicts": ["SEMANTIC_PACKAGE_CONTRACT_ACCEPTABLE", "BLOCKED"]
  },
  "deployment_impact": {
    "classification": "database-free package-contract only", "highest_claim": "local wire and consumer constructibility", "deployable": false,
    "unchanged": ["product", "tests", "migrations", "runtime", "configuration", "dependencies", "services", "providers", "frontend", "infrastructure"]
  },
  "nonclaims": [
    "No PostgreSQL behavior, semantic fact, real semantic package, DATA/CAT obligation, migration behavior, foundation acceptance, deployability, release, production, live, order, or money claim follows.",
    "This capsule cannot activate its successor or authorize a PostgreSQL command.",
    "It consumes root-accepted source-derived fixture and command values without changing them; its ownership starts at generic PQ wire/API, parser, digest, provenance, and protocol-package contracts."
  ]
}
---

# PostgreSQL 16 semantic package contract

This blocked serial successor owns only the recursive wire and exhaustive independent-consumer boundary. Root acceptance of the completed trigger-fixture predecessor is its only activation route.
