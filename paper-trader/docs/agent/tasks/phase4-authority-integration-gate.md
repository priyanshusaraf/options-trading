---
{
  "id": "phase4-authority-integration-gate",
  "phase": "phase4",
  "status": "accepted",
  "goal": "Revalidate the complete Phase 4 authority chain after CUR-C1 through CUR-C4, CUR-H1, and CUR-H2 close, without patching product code or claiming review readiness.",
  "goal_contract": {
    "create_before_work": true,
    "owner_authorization": "The accepted Phase 4 authority-foundation architecture correction authorizes this evidence-only serial integration gate after all authority corrections close, including the bounded capability-assessment receipt correction routed from its first Critical stop, the timestamp-normalization correction routed from its second Critical stop, the forward research JSON-shape parity correction exposed by the preserved PostgreSQL integration path, the root-accepted retained loader-test fixture correction from its third stop, the bounded owner-isolation fixture correction routed from its fourth stop, and the current-authority acceptance-scenario fixture correction routed from its fifth stop. Authority is limited to the listed integration test and evidence paths; product patches, programme transitions, review-readiness claims, provider adapters, frontend, deployment, production, live, and money boundaries remain closed.",
    "stopping_condition": "Complete only after the real execution and research loaders revalidate the full canonical authority chain across process death and both supported databases; all 27 adversarial rows have separate owned real-path refusal or identity-change evidence; every transitive stale claim is revalidated or remains explicitly blocked; CUR-H3 and CUR-H4 forbidden consumers remain unreachable; and protected and scoped-path checks pass. Stop with the goal active if any Critical or CUR-H2 finding remains, evidence is missing, or integration needs a product patch."
  },
  "risk_tags": ["critical", "integration", "fresh-process", "postgresql", "evidence"],
  "required_docs": [{"path": "paper-trader/docs/superpowers/specs/phase4-strategy-os-design.md", "sections": ["10. Acceptance scenarios and refusals", "11. Deployment contract", "13. Authority-foundation correction contract"]}, {"path": "paper-trader/docs/superpowers/plans/phase4-strategy-os.md", "sections": ["5. Verification cadence", "8. Authority-correction serial route"]}, {"path": "paper-trader/docs/agent/DEFECT_PATTERNS.md", "sections": ["DP-001 — Distinct facts collapsed into one representation", "DP-002 — Syntactic self-consistency mistaken for authority", "DP-003 — Mocked seam presented as lifecycle evidence", "DP-004 — Immutable envelope over mutable or time-incoherent facts", "DP-005 — Address-bearing metadata mistaken for a typed authoritative fact", "DP-006 — Database session timezone changed copied authority instants", "DP-007 — Cross-dialect JSON constraint changed authoritative field shape"]}],
  "dependency_gate": "phase4-acceptance-scenario-fixture-correction",
  "allowed_paths": [
    "paper-trader/backend/tests/test_phase4_authority_integration.py",
    ".agent/runs/phase4-authority-integration-gate"
  ],
  "nonclaims": ["Integration evidence is not independent critical review, programme closure, deployment, production rehearsal, provider correctness, live, or money authority.", "The gate patches no product, migration, existing test, programme, current-stage, or review-package path."],
  "owner_gates": ["Stop if any Critical or CUR-H2 is open, any earlier capsule evidence is missing, H3/H4 forbidden consumers are reachable, or integration needs a product patch."],
  "stop_conditions": ["Any stale contract lacks direct recheck; any authority/persistence/reload/consumer seam is monkeypatched; either PostgreSQL plane is omitted; a mutation survives; protected hashes change."],
  "deployment_impact": {"classification": "local-two-plane-authority-integration-evidence", "required_evidence": "Exact execution 0039 and research 0010 heads; SQLite compatibility plus isolated PostgreSQL 16; no deployment/config/service/provider/credential/live action; unresolved release obligations retained."},
  "model_route": {"owner": "gpt-5.6-sol", "owner_reasoning_effort": "medium", "service_tier": "default"},
  "parallel_budget": 0,
  "assignments": [],
  "adversarial_rows": ["ADV-001", "ADV-002", "ADV-003", "ADV-004", "ADV-005", "ADV-006", "ADV-007", "ADV-008", "ADV-009", "ADV-010", "ADV-011", "ADV-012", "ADV-013", "ADV-014", "ADV-015", "ADV-016", "ADV-017", "ADV-018", "ADV-019", "ADV-020", "ADV-021", "ADV-022", "ADV-023", "ADV-024", "ADV-025", "ADV-026", "ADV-027"],
  "acceptance": [
    "real create/admit, persist, process death, reload, verify, and consume/refuse crosses execution and research planes with no critical monkeypatch",
    "exact canonical bytes/address/owner/mode/product/contract/dependencies match across planes",
    "all 27 design §13.3 adversarial_rows have separately recorded real-path evidence and are killed; no grouped row, missing row, or unowned row passes",
    "equity, weekly-options, and cross-market scenarios recheck causal completed-bar and net identity contracts at the supported local boundary",
    "SQLite and disposable PostgreSQL prove exact heads, clean install, supported upgrade, restart, atomicity, and refusal parity",
    "the transitive invalidation register names direct evidence for every stale test/report/scenario/cache/result claim and retains CUR-H3/H4 blocks",
    "the real terminal consumer remains V2_RUNTIME_UNAVAILABLE and no Phase 4 closure or review-readiness claim is made"
  ],
  "test_plan": ["Run and record every adversarial_rows ID separately through its named real seam; the ledger must name database/process boundary, expected refusal/identity change, observed result, artifact address, and originating capsule evidence; one logged integration suite plus migration and architecture/source-map validation; protected hashes; frontend and scoped diff checks."],
  "review": {"required": false, "assignment_id": "phase4_authority_integration_owner_6", "agent": "owner", "model": "gpt-5.6-sol", "reasoning_effort": "medium", "base_sha": "HEAD", "package": ".agent/review-package.json", "review_paths": ["paper-trader/backend/tests/test_phase4_authority_integration.py"], "exclude_paths": [], "output": ".agent/runs/phase4-authority-integration-gate/owner_6/report.md", "verdicts": ["INTEGRATION"]}
}
---

# Phase 4 authority integration gate

The first run stopped correctly because the persisted receipt embedded an
incomplete legacy assessment projection while binding the tagged typed address.
That bounded correction is root-accepted. The second run then proved all 27 ADV
rows and the SQLite lifecycle but stopped when PostgreSQL under Asia/Kolkata
changed an aware canonical provider-contract instant at a timezone-naive copied
SQL boundary. The accepted timestamp repair cleared that seam, and the
preserved PostgreSQL path then exposed a separate research JSON-shape split:
one shared constraint meant syntax-only on SQLite but object-only on PostgreSQL,
including five legitimate array-valued manifest fields. The third run cleared
those product seams but stopped because a retained loader-tamper test indexed a
retired assessment path and raised before invoking the real loader. That
test-only correction is root-accepted. The fourth run cleared that retained
loader seam, then stopped because a retained research owner-isolation test
called an obsolete fixture argument and raised before real persistence. The
bounded owner-isolation fixture correction is root-accepted: the retained test
now builds a genuine current typed owner-b authority chain, reaches real
research persistence, and preserves exact retry, owner isolation, collision
refusal, and default owner-a compatibility. The fifth owner then passed the
current SQLite and PostgreSQL lifecycles, all 27 ADV rows, retained persistence
tests, current integration-owned scenarios, and H3/H4 containment. It stopped
because three retained acceptance scenarios still called the retired
`assessment=` interface and raised before admission. Their bounded test-only
correction is root-accepted, including an exact persisted weekly-contract
listing fact and the genuine unavailable order-flow refusal. The sixth fresh
owner passed the complete gate from current bytes without exclusions or
deselections: all 27 ADV rows, SQLite and isolated PostgreSQL 16 authority
lifecycles, retained tests and scenarios, execution head `0039`, research head
`0010`, and CUR-H3/H4 containment. The root accepted that bounded evidence at
`.agent/runs/phase4-authority-integration-gate/root_acceptance/acceptance.md`.
All five partial runs remain historical evidence only, and the sixth result is
not a Phase 4 closure or review verdict.
Recheck each stale contract through real
loaders and databases. Every design §13.3 row ID is a separate required
integration case; references may join earlier artifacts but cannot replace a
real integration observation. Record bounded evidence and unresolved
obligations. A later root-routed independent critical review owns any
review-readiness verdict.
