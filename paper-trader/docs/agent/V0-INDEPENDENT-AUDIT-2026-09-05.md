# V0 independent audit and correction status

Date: 5 September 2026. Scope: the owner's research-and-alerts journey using OHLCV, including cross-instrument conditions. Verdict: **not ready for launch**. This verdict describes concrete missing behavior and evidence; it is not a demand to restart accepted work.

## Evidence boundary

The audit inspected the current dirty backend checkout and the separate `dev/strategy-os-frontend` product, not just the old phase reports. Current source inspection and selected focused tests do not prove a deployed release. The inherited baseline is preserved under `.agent/runs/v0-launch-reset-2026-09-05/baseline/`. No commit, push, production access or deployment has been performed for this programme.

## Findings and actions

| ID | Evidence and user effect | Action and acceptance |
| --- | --- | --- |
| V0-01 Instructions | The old startup test failed because CURRENT.md was 140,602 bytes against a 4,096-byte limit. Named workers refused direct authorized work because of unrelated capsule restrictions. Project plugin discovery hid installed Ponytail. | Corrected: short WORKING-PLAN/router, explicit-assignment support, retained historical state, plugin discovery. Existing 9 repository-contract and 33 hook tests passed; six changed skills passed format validation. Final complexity review still required. |
| V0-02 Data entry | The real frontend lists datasets but cannot import/register one from a clean account; DataLibraryWorkspace is unmounted. Static scopes remain capability-blocked. | Implement an owner-scoped OHLCV import/selection and static research scope using the existing canonical store. Prove compatibility and specific rejection through the served application. No broker capability is implied. |
| V0-03 Cross-instrument research | Saved-V2 API/operation/projection/admission bind one manifest. `_graph_input_fields` accepts one `frame`; analytical binding can force a benchmark frame to primary. Two independent backtests would not satisfy the requirement. | Reuse existing multi-dataset eligibility and canonical V2 runtime. Bind every graph input to its dataset/instrument and include all identities in admission/results. Prove that NIFTY changes a SELF decision; test future, absent, stale and misaligned external observations. Architecture map exists; implementation remains open. |
| V0-04 Research validity | Qualification evaluates full-dataset net P&L before the same observations are called OOS. Passing fold tests do not prove selection independence. | Active correction: explicit recorded development/holdout partition, development-only qualification/selection, common holdout for gates, scoring, charts and robustness. Verify unchanged development decisions when holdout prices change, causal warmup and strict saved-V2 compatibility. Independent review reproduced additional canonical-order split, premature challenge and empty-holdout failures, plus evidence/selection explanation defects. Their correction is active; the earlier passing checks do not close these findings. |
| V0-05 Monitoring and alerts | Monitoring read/interaction factories are absent from `app.main` mounts. No monitor worker entry point is packaged. AlertsInbox/AlertDetail are unmounted and lack production API-client routes. | Connect the existing monitoring producer, repository, routers and UI. Prove a supported transition from clean account to attributable alert and cross-user denial. Reading/acknowledging must not rewrite signal evidence. Enable only proven capabilities. |
| V0-06 Retention | Existing pruner covers legacy telemetry, not a complete product retention/deletion lifecycle across identities, strategies, datasets, research, alerts, exports and backups. | Define each record's required retention, deletion or immutable exception and backup behavior with accountable ownership. Then implement and test scoped lifecycle behavior and restore non-reappearance where promised. Do not delete money/audit facts to satisfy a generic expiry rule. |
| V0-07 Explanation and UX | A created V2 graph can be edited/published, but the frontend startExperiment still calls the legacy GraphVersion endpoint. The saved-V2 operation endpoint requires a pre-existing admission; no public API producer was found. The integration test constructs it directly with fixture evidence. Known capability limits need useful reasons. | Add a real saved-V2 compatibility/admission producer, connect its asynchronous operation/status path, and replace the legacy launch call. Do not fabricate evidence addresses or route V2 through a legacy engine. Complete reason mapping alongside calculations and use exact revision/data/evidence labels. Independently observe create/preset, edit, compatibility, run, rejection, challenge, compare and saved evidence. |
| V0-08 Release and rights | No exact-candidate clean-account deployed journey exists. Provider activation is intentionally denied pending conformance and rights. User-owned keys do not settle commercial rights. | Resolve [provider/data permissions](PROVIDER-RIGHTS-2026-09-05.md), complete candidate security/recovery checks, obtain deployment approval, then repeat the journey on that build. |
| V0-09 Backup TLS | The database article review followed recovery into `postgres_process_spec` and reproduced loss of URL TLS settings without a DB connection. | Corrected: explicit TLS policy, ambient target/password overrides, error redaction and temporary-file cleanup. Independent SPEC/QUALITY pass; 27 focused cases and eight targeted security mutants pass at their stated scope. Remote TLS handshake/restore remains unverified. |
| V0-10 Test evidence privacy | New backup tests in this workstream captured the host environment. Two failed-test logs contained `ANTHROPIC_API_KEY`, and a reviewer tool output included the value. | Logs sanitized in place; tests now use dummy-only environments. Owner notified and advised to revoke an active key. Owner confirmed the key is revoked on 5 September 2026. No replacement is needed. No raw key is retained in this audit. |

The [eight-article database review](../engineering-references/reading-packets/2026-09-05-planetscale-database-failures.md) also adds bounded verification for effective workload timeouts, nested savepoint batches, aggregate connection demand and realistic restore duration. It found no current session-read-only poison source and no measured need for sharding or Neki.

For V0-10, hash-only sanitization evidence and the owner-confirmed revocation status are in `.agent/runs/v0-launch-reset-2026-09-05/credential-log-incident/sanitization.json`. Removing local values cannot retract a past tool-context exposure. The two old backup failure logs are superseded; use the regenerated dummy-only mutation logs for review. This was a defect in our new test harness, not evidence of a production database leak. Other exposure and key usage were not investigated.

The independent launch-path report has exact source lines and missing mounts at `.agent/runs/v0-launch-reset-2026-09-05/launch-audit/REPORT.md`. The cross-instrument implementation map is in the run's `cross-instrument/report.md`. Neither is a runtime pass. The repository architecture validator also passed with `--root .`; its full result is in `instructions/architecture-validation-with-root.log` under the same run.

## Programme and release reconciliation

| Recorded programme area | Recorded status | Meaning for this V0 |
| --- | --- | --- |
| Phases 1–4 | Accepted foundations in the revised mapper | Reuse their canonical identity, tenancy, data and execution boundaries. Recheck affected behavior; acceptance is not blanket launch proof. |
| Phase 5 | Implementation and review recorded accepted | Sizing/admission foundations remain relevant to accounting and future execution. They do not prove the new customer journey. |
| Phases 6–8 | Programme stages blocked | Language, temporal/resource, discovery/reference and provider-coherence roadmap. Required bounded OHLCV/cross-instrument behavior still needs delivery under the latest V0 mandate. Dynamic discovery is deferred. |
| Phases 9–11 | Programme stages blocked | Chart/thesis, research integration and integrated frontend roadmap. V0 has separate partial implementations; audit actual routes and outputs instead of treating these as all absent or all complete. |
| Phases 12–13 | Programme stages blocked | Security, operations, benchmarks and release hardening remain release obligations where V0 uses them. They cannot be deferred solely because the old phase number is later. |
| V0 vs V1 | Later reconciliation makes V0 research/evidence/monitoring/alerts and V1 controlled execution | The owner's latest journey is controlling. Options/order flow and all-30 readiness are excluded now; public live execution stays closed. |

Sources: preserved `programme/PROGRAMME.json`, `CURRENT.md`, revised V1 progress mapper and 29 August V0–V6 reconciliation. This is a current implementation/scope reconciliation, not a new claim that every historical source has been independently revalidated.

## Golden strategy and design work

The archive has 30 prompts plus a README. The owner also requested separate original Trend Impulse V3 (EMA50 slope/z-score) and Expanding Z Impulse V4 presets. V3 is not a standalone prompt in the archive; prompt 28 is a different rotation strategy. Verify both originals against independent arithmetic and current IR before exposing them. Prioritize reusable trend rules with a separately bound NIFTY benchmark, historical gap state and target-overlay behavior, and price-only break/retest cases. A prompt is not executable proof. Record missing generic primitives and exact expected traces before implementation; do not add symbol/date-specific production exceptions.

One year of official NIFTY daily OHLC is acquired and structurally checked. Intraday/volume/other instruments and additional warmup remain separate needs. Historical-gap replay also requires a frozen TrendState and a declared starting gap registry. Do not invent the owner's trend rule to force the expected result.

The separate [research-workbench concept](../../design-lab/research-workbench/index.html) explores a rule-sheet editor and an evidence-led journey. Example values are labelled; it has no production engine or data connection. Browser inspection found and corrected misleading empty-draft and navigation behavior. The corrected example journey, disabled-state handling and narrow-screen layout have browser evidence; the root also inspected the desktop rule sheet. Existing product frontend integration remains separate.

## Quality and next verification

Apply the owner's strict thresholds to changed/new code. High-risk CRAP <10 cannot be met by coverage alone when cyclomatic complexity is 10 or higher. Measure the actual function scope, tool/version, coverage and mutation outcomes. No whole-product zero-dead-code, zero-redundancy or zero-survivor claim has been established.

Next: finish and independently review the research separation correction; measure its complexity/coverage and kill its relevant mutants; implement canonical cross-instrument bindings; then complete the data-entry and monitoring/alert path with clear reasons. Keep subsequent release and external-permission gaps explicit while continuing independent local work.
