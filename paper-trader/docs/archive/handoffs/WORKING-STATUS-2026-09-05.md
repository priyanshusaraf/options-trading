# V0 continuation status — 5 September 2026

Moved intact from WORKING-PLAN.md during the instruction audit. These are recorded observations, not fresh verification. Recheck process handles and shared ownership before resuming.



Evidence: `.agent/runs/v0-launch-reset-2026-09-05/`. CSV limits and public research preparation are implemented. The live 248-row import exposed an observation-list and byte-limit mismatch. Corrected HTTP publication and reload checks pass for 248 and 2,000 rows. Browser retry awaits the existing certificate prompt and an unlocked Mac. The local API is process 20815; frontend process 24644 serves the repository-owned V0 package.

Settings histories and /3 pinned submission/recovery pass backend and frontend checks, independent scoped review and selected mutations. Local0052 migration retained records; PostgreSQL/restore proof remains open. Main Account/Build settings controls were browserchecked. New presets open Build; full combined adjustable studio remains unfinished.

Both presets have signal parity. Named V4 reversal/fixed-unit replay passes35focused cases,1000legacy differential tapes and policy identity checks; full historical fill parity remains open. Cross-instrument reference is ready (16primary conditions,6allowed,10benchmark-blocked), and benchmark EMA compilation, state replay and snapshot restore now pass the role correction checks. The analytical slice covers 67 unique cases across its main run and one corrected expectation; independent review passed for this analytical correction. Saved-version library revalidation and the full cross-input journey remain separate integration requirements.

Profiling confirmed repeated implementation identity reconstruction dominated research preparation. Reusing source, closure and instruction reads within each validation reduced the measured worker profile from 234 to 162 seconds while preserving the checked identities. Independent review passed; this single paired profile is not a capacity claim.

The V0 frontend now lives in `paper-trader/strategy-frontend`. A clean install, 464 frontend tests and production build passed before the current Save version changes. The explicit V0 deployment selector and asset-preservation guard passed independent review and 30 local checks. No deployment occurred. Browser verification of the built artifact and the deployed journey remain open.

Active integration: staged-edit Save version handling, immutable publication receipts and explicit library revalidation (migration 0053), and cross-instrument analytical binding. Keep the owner database on 0052 until the new migration has passed its checks and a backed-up local upgrade is ready. Root owns the next canonical input-set projection and Phase 5 authority work. The legacy single-dataset projection now refuses multiple input names. A persisted regression reproduced the missing refusal and passed after the one-line guard; singleton benchmark projection remains supported. The changed function passes the measured quality gates and one selected boundary mutant was killed. Independent review passed for the scalar guard. The new exact input-set producer and Phase 5 wrapper now pass ten focused persisted-source, legacy-manifest and scalar-compatibility cases. Review-driven clock and cutoff proof corrections are implemented. A surviving mutation exposed empty-field DatasetManifest equality; the shared equality/hash correction now distinguishes canonical and legacy facts correctly. All 18 affected functions pass quality gates, and all eight selected mutations are killed with fresh-process baseline/restored checks. Final independent review passed for the input-set producer, verifier and manifest equality correction. The new path is not yet connected to policy, admission, queued preparation or the UI. Do not claim the cross-instrument journey complete.

Cross-input integration, insertable strategy components, staticwatchlist, challenge/revision/evidence, monitoring/alerts, retention/recovery and the deployed clean-account journey remain required. No launchpass.


Saved-version integration now includes staged semantic/layout save, exact readback and publication, plus a deliberate “Save updated version” action for changed components. Focused frontend checks and selected mutations pass; actual browser proof remains pending. Backend publication/migration checks passed 24 SQLite cases; the worker is finishing scoped coverage and mutation evidence. Do not upgrade the owner database or claim readiness before that evidence and independent review are complete.


Current cross-input continuation: the distinct Phase 5 evaluation policy `/2` and result `/2` are implemented but not yet accepted. The pending persisted test executes separate primary and benchmark EMA nodes and checks source-policy rebinding refusal. The capability worker has implemented a new source-bound assessment `/3` on the unchanged full plan; runtime and mutation checks are pending. Its contract keeps original source-local provider roles and verifies their explicit graph-role mapping.

A lifecycle collection attempt refused a stale implementation identity for `intent.account_equity` while capability source was changing. A frozen-source diagnostic is running on handle 20423 (runner process 33178); do not bypass that guard or count the failed collection as test evidence. The intent catalogue fingerprints the capability module, so freeze that transitive source during registry imports. After the diagnostic, finish the pending policy helper complexity split, run the input-set execution/legacy-clock checks, then release the backend slot for capability and lifecycle closure checks.
