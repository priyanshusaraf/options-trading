# Continue V0: alerts, canonical search and session binding

The full approved programme is unfinished. Continue in this exact checkout on `codex/execution-foundation`; preserve all inherited edits. Main Astra medium, every child/reviewer Astra low, default role, `fork_turns: none`. Do not substitute models, commit, push, deploy, use live credentials or grant execution authority. The old source task remains read-only. This handoff supersedes the status portions of HANDOFF-2026-09-05-SETTINGS-STUDIO.md.

Repeated selected-model capacity failures interrupted the canonical-search and alert-UI workers. All writers are now frozen. Last process inventory showed no pytest, coverage, run_logged or Vitest process. Do not duplicate old checks. The saved programme goal reported BLOCKED after capacity failures; it is not complete. Continue the authorized work when the requested model is available.

## Runtime

- Local API PID69648, launcher `.agent/local-owner-runtime/start_backend.py`, port8090; frontend PID61777, HTTPS5187. Preserve their local configuration and the owner's existing auth. Current API health reports ready, current/head0053, `v0_research_signal`, roleAPI, execution_authority=false. No broker cell runs in this profile.
- Configuration checks confirmed mock/paper, dotenv disabled, empty live acknowledgement, local databases and mode0600. Never print runtime.json or secrets. No migration was added or rerun.
- API69648 loaded the completed percentage/account/gap source before subsequent canonical-search /3 and monitoring-publication edits. **Do not restart until /3 backend and frontend parsing are integrated and verified.** Frontend currently does not support /3.
- Computer Use reported the Mac locked. An unlock request is pending; actual signed-in browser journeys are still open. Do not fabricate sessions or bypass the lock.

Evidence below is under `.agent/runs/v0-launch-reset-2026-09-05/`.

## Completed source and verification

- `settings-v2/RESULT.md`: actual SL/TP fractions, close-confirmed after entry bar, next-open fills, bands based on slipped entry. New /2 snapshots pin5bps round-trip fill spread; /1 snapshots retain old semantics/bytes. Old save/preparation retries and canonical numeric-alias validation corrected. Real two-source64-session preparation→interruption→recovery→one ExperimentRun passed both disabled-band and1%/2% cases.27 function gates pass;13 root mutants killed plus independent consumer probes. Independent SPEC/QUALITY PASS. These results precede the unfinished /3 changes.
- `account-risk/RESULT.md` and independent-review: nonrelaxable owner limits applied separately per account/book, including new max_daily_profit INR(default0). Fixed limits halt new entries while breached; exits remain available. Giveback retains its existing flatten/latch behavior. Fixed short-futures MTM sign and futures giveback settlement; profit notices no longer suppress later loss notices.24 final function gates and13 mutants pass; correction25 cases +11 contract cases pass. Legacy giveback latch/high-water remain process-local, explicitly not a durable execution-readiness claim.
- `settings-ui-quality/RESULT.md`, `account-risk-ui-quality/RESULT.md`: dedicated global and strategy research Settings, percentage fields, provenance/clear-to-inherit and reviewed retained-operation same-data rerun; account entry-limit UI uses only3 whitelisted fields and correct role/CSRF boundaries. Earlier293/194 focused executions, typecheck and selected mutations pass. Arbitrary historical multi-input rerun remains unsupported when original mapping/cutoff cannot be reconstructed.
- `signal-settings-ui-quality/RESULT.md`: in-place signal parameters now use actual canonical validate→mutate CAS→publish→readback, including clear_parameter, current version and owner-context cancellation. Shared helpers extracted into builderParameterContracts.ts and reused by Builder.96 focused routes/Builder executions,12 mutants,66 function gates and final typecheck pass. Independent authoring review and actual browser save/backtest still pending.
- `studio-integration-quality/RESULT.md`, `studio-watchlists-quality/RESULT.md`: actual runs/chart/builder three-pane Build, resize/maximize/reset, compact Runs/Watchlists switch, verified member history and backtest context. No automatic run or whole-list execution.75 final watchlist executions,10 selected mutants and60 function gates pass. Earlier prototype panning/Fit/focused-button guards and Type1–5 strips passed. Browser QA remains.
- `historical-daily-gap/RESULT.md`, `quality-final.json`, independent-review: canonical `structure.historical_daily_gaps/2`, immutable gap geometry, monotone state, bounded4096 refusal, synthetic16-node eligibility/target-delta oracle, persisted fresh-process restart. Registry456/public270/Type3=64. Nested streaming-reference support added. Review found invalid nested clocks; corrected shared aware/monotone/unique clock predicate, eight focused cases and two guard mutants pass.47 current unique function gates pass. No full core/overlay trade bridge is claimed.
- `settings-studio/build.log`: production build passed before later in-place signal/alerts/optimizer changes. Do not call this the final build.

## Unfinished canonical optimizer: preserve partial implementation

Previous worker `pending_review` was interrupted by capacity. No runner remains. Approved exclusive paths: research/domain/settings.py; app/api/research_settings_routes.py and ir_experiment_routes.py; research/orchestrator/{v2_preparation,v2_operation,run}.py; research/pipeline/optimize.py; new research/pipeline/v2_parameter_search.py; research/robustness/parameter_integration.py; app/core/research_read.py terminal evidence seam; existing tests.

Design is approved KEEP+HARDEN: /3 settings add `{optimization:{schema:"canonical-local-development-search/1",enabled,axes:[{node_id,parameter_id,step,minimum,maximum}]}}`; normalized decimal strings,1–4 unique sorted numeric axes, baseline±step bounded to81 points. Workspace cannot enable graph-specific axes. Preserve /1,/2 bytes. New /3 pins the fill spread. Pure prequeue canonical edits validate axes; no candidate runtime under the DB lock. Reuse canonical candidate construction and existing nested development selection/final selection/PBO; selected immutable runtime with empty overrides alone reaches locked holdout. Persist every candidate/trial, selected canonical document/hash/lineage and cancellation/failure population. Never auto-publish it or fit holdout.

Partial /3, queue, shared optimizer, selected-runtime holdout and read-side reconstruction code exists but is **not verified complete**.19 focused settings/legacy cases passed before a test-only missing import was fixed. Latest `canonical-search/search-first.log` fails because `_canonical_test_signals` in research_tests/test_optimize.py reads `candle.volume` from the legacy Candle fixture, which lacks that attribute. Fix that fixture coherently, then proceed with holdout-perturbation, actual3-point runtime, failure/cancel/recovery, semantic evidence reconstruction, metrics/mutations and independent review. No81-point heavy run is needed. Frontend /3 controls/parsers are not implemented.

## Unfinished alert publication

Root-owned edits: app/monitoring/publication.py, app/api/monitoring_read_routes.py, app/api/request_actions.py, app/main.py, app/core/release_profile.py, tests/test_v0_monitoring_read_routes.py and tests/test_api_auth.py.

`monitoring-inbox/published-focused.log` has22 passing focused cases, including real application middleware, durable two-owner sessions, attention, own-review readback, viewer downgrade/revocation and no-store. Published GET list/detail uses `monitoring-alert-page/2` / `monitoring-alert-read/2` with actual monitoring_event_address,strategy_id,graph_version_address,last_sequence. Factory-default /1 remains. GET `.../review` returns `{schema:"monitoring-review-read/1",review:null|{review_address,assignment_id,monitoring_event_address,disposition,reason_code,note,created_at}}` for the current reviewer only. Existing POST attention/review routes are registered. Signals capability advertises stored history/reviews only; activation remains blocked. Source is not loaded in API69648 yet. Final scoped quality/mutations/review and readiness/profile checks remain.

Alert UI worker `studio_integration` hit capacity during contract mapping; no new client/workspace was written. It may resume owning features/alerts, shell/api.ts, product/routes.tsx and PrecisionShell navigation. Existing AlertsInbox/AlertDetail are still views only. Reuse same-origin transport/CSRF/abort, closed response identities and actual sequence, owner/member write:review vs viewer read-only, expired-state display, pagination and own-review readback. Omit unread_only for All/Active; false means read-only alerts. Server notes are trimmed/control-free; align the form without silently altering typed input. No guessed sequence0, fake alerts, auto activation or execution.

## Daily-session proposal awaiting implementation

`session_binding` made no edits. Its approved-to-consider minimal proposal needs final scope confirmation: optional explicit USER_DECLARED session metadata sidecar through existing RawObservationSegment + MarketTruthSnapshot.source_evidence; no schema/dependency or CSV-byte rewrite. Proposed files research/data/{user_csv_import,imported_sessions,canonical_dataset}.py and existing research_tests/test_canonical_dataset.py. Require one-to-one source label coverage, full-session declaration, unique IDs, exact timezone labels, increasing non-overlapping sessions and close<=label+86400. Preserve current alignment label and NOT_SUPPLIED historical source availability. Projection emits SESSION_* only from verified owner/product-bound persisted metadata. Public API/UI sidecar input remains to be integrated.

Gap node also needs a separately tested late-evaluation correction: accept evaluation>=session_close and compare overlap against previous session close, not previous evaluation time. There is no authoritative historical exchange-calendar producer; market_hours omits holidays. Never invent NSE close times or relabel source timestamps as official availability.

Source007 immutable NIFTY geometry remains [23823.60,23891.55] from July27, first proximal/distal range reach September2. CSV is valid. Default V3/V4 short pulses at September2 close cannot explain an earlier entry. Full historic TrendState/ATR/stop/cooldown/addition rules and core/overlay-owned target-position backtest bridge remain required; do not fabricate fills or date-fit.

## Immediate next steps

1. Resume requested-model workers or continue locally from these frozen files; inspect handles first.
2. Fix/verify partial /3 optimizer and integrate its Settings/results UI before restart.
3. Complete alert UI, then real saved-strategy monitoring/source activation and attributable alert lifecycle; existing factories alone are not completion.
4. Complete explicit session binding and target-position bridge where authorized; retain honest historical limitations.
5. Finish final build, independent critical reviews, signed-in import/save/backtest/settings/search/compare/evidence/monitoring/alert journeys and readiness. No deployment/live/commit authority.

Root also patched frontend catalogue parsing to accept the exact new270-node gap catalogue while preserving prior269/243 forms in shell/contracts.ts and its existing test. All64 existing contract cases passed after the narrow fixture correction; its scoped metrics/mutations remain pending. This fixes a real stale269-only consumer that would otherwise reject the running backend's catalogue.
