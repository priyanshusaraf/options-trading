# Product work

- The V0 product UI is in `strategy-frontend`; `frontend` contains the legacy bot UI. Use `docs/agent/WORKING-PLAN.md` when scope or priorities are unclear.
- Bind research results to the exact strategy revision, data, implementation and assumptions. Preserve that identity in caches.
- Keep builder, research and monitoring behavior consistent. Report missing, stale or unsupported data clearly.
- Preserve owner settings and alert evidence across edits and restarts.
- Use trader-facing language. Label example data and mocked behavior.
- Verify the affected user journey through its real interface and backend before calling it integrated. Distinguish local verification from deployment.
