#!/bin/sh
# Keep the local whitespace check; AI review is optional.
git diff --cached --check || exit $?
if [ "${STRATEGY_OS_AI_REVIEW:-0}" = "1" ]; then
    exec /usr/bin/python3 "$(git rev-parse --show-toplevel)/.codex/hooks/ponytail_review_guard.py" commit
fi
