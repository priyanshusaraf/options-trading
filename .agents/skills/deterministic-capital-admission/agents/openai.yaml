interface:
  display_name: "Strategy OS: Verify capital admission under contention"
  short_description: "Test deterministic reservations under contention"
  default_prompt: "Use $deterministic-capital-admission for the relevant Strategy OS task, preserving the active capsule and owner gates. Report evidence, gaps and limitations."
