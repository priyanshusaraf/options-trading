# Runtime and UI review

For local runtime evidence, confirm mock provider, paper execution, empty live acknowledgement, disabled dotenv, temporary database, and no ARM. Never accept a local check that used the shipped environment or live state.

For UI changes, exercise the affected operator flow in a running browser, inspect console and network failures, and check loading/error/empty states. Use the assignment's supported layouts; inspect 390×844 only when mobile is in scope. Screenshots support the observed behavior. Check that displayed values are truthful or visibly unknown, REST remains centralized, and no second WebSocket exists. The current assignment authorizes bounded frontend implementation.
