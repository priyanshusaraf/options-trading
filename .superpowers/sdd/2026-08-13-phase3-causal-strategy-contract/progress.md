paper-trader/docs/superpowers/plans/2026-08-13-phase3-causal-strategy-contract.md

# Phase 3 causal strategy contract execution ledger

- Starting commit: `5b8ae1d`
- Protected inherited files: `app/engine/kite_venue.py`, `app/engine/venue.py`, `app/providers/brokers.py`, `tests/test_broker_registry.py`
- Review rule: each task requires observed RED, focused GREEN, independent specification and quality review, protected-file hash check, and a scoped commit.
- Research rule: substantial slices consult at most three relevant read-only repository reviews, then the exact named upstream modules, and record the fit without importing research repositories.
- Verification rule: classify each task as Critical, Important, Routine, or Trivial before implementation. Add tests only for a concrete failure hypothesis, run affected tests during implementation, subsystem suites at commit boundaries, and broad suites at phase/release boundaries. After two failed test/fix loops outside a Critical area, reassess whether the test protects a real requirement. Preserve maximum rigor for money, tenant isolation, irreversible state, authentication, and backtest-to-live strategy parity.

## Compact research provenance

The owner-authored 2026-08-13 Strategy OS product, node-language, market-truth, deployment-trust,
runtime-economics, and verification steers are now governing requirements. Their reconciliation is
recorded in `paper-trader/docs/superpowers/specs/2026-08-13-strategy-os-v1-product-steer-design.md`
and the ten-phase dependency contract in
`paper-trader/docs/superpowers/plans/2026-08-13-strategy-os-v1-master-sequence.md`. For Phase 3 they
narrow the claim rather than widen Task 4: causal admission is necessary but not complete Strategy
Preflight; numeric validity, point-in-time market truth, provider capability, role binding, dynamic
derivatives, and resource planning remain explicit later-phase gates.

| Slice | Blocked capability | Sources inspected | Adopted pattern | Reuse | Decision |
| --- | --- | --- | --- | --- | --- |
| Task 1 causal declarations | Exact recursive per-bar state and closed construction-time validation | XState review plus `createMachine.ts`, `StateMachine.ts`, `StateNode.ts`, `transition.ts`; Dagster review plus `graph_definition.py`, `dependency.py`, `composition.py`, `definitions_class.py`, `assets/graph/asset_graph.py` | Pure `(state, event) -> (next state, outputs/actions)` transitions; definitions stay separate from invocation; reject incomplete or conflicting dependency declarations before execution | Reference-only; XState MIT, Dagster Apache-2.0; no copied code or dependency | Keep Component IR v1 and implement native immutable recursive contracts. Do not replace the registry, add an actor runtime, or treat a DAG framework as strategy authority. |
| Task 2 implementation identity | Immutable callable identity that changes when any accepted helper, state transition, module, or dependency changes | DVC Data review plus `hashfile/obj.py`, `hashfile/db/__init__.py`, `hashfile/cache.py`, `hashfile/status.py`, `hashfile/transfer.py`; Dagster review plus the Task 1 graph/definition modules | Separate logical names from immutable content identity; keep caches as accelerators rather than authority; keep definitions, invocation, and validation distinct; reject incomplete dependency closure | Reference-only; DVC Data and Dagster are Apache-2.0; no copied code or dependency | Derive a native semantic implementation address from callable/state/dependency closure. A body ref or source-file digest alone is insufficient, and manual dependency tuples are inputs to validation rather than trusted truth. |
| Task 3 structural admission | Immutable owner-bound decision receipt over exact graph, implementation, provenance, mapping, history, and risk facts | DVC Data review plus Task 2 hashfile modules; OpenFGA review plus its versioned-model/check command modules named in the review | Bind a decision to immutable versioned inputs; validate the model before activation; keep mutable aliases and roles separate from decision identity; fail closed on incomplete facts | Reference-only; both Apache-2.0; no copied code or dependency | Build a native Strategy OS structural receipt stronger than a byte hash or authorization check. Owner scope is hashed evidence, while authentication and tenant query enforcement remain separate. |
| Task 4 prefix/vector parity | Independent detection of whole-frame lookahead and exact deterministic recursive transitions | Strategy OS reuse audit; VectorBT review plus `indicators/factory.py`; XState review plus `packages/core/src/transition.ts` | Compare the optimized whole-array path against an independently implemented completed-prefix oracle; keep recursive transitions pure and ordered; bind the immutable fixture suite and decision bytes | Reference-only; VectorBT Commons Clause prohibits product dependency, XState MIT but no reuse needed | Implement a native independent evaluator and adversarial admission gate. Reject VectorBT runtime generation/in-place outputs and XState as the IR. |

| Task | Status | Commit | Evidence |
| --- | --- | --- | --- |
| 1. Closed bounded and recursive causal declarations | completed | `ba6ed7c` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Exact reproduced parity RED/GREEN recorded in `task-1-report.md`; 253 passed, 6 skipped inventory; compile/diff/protected hashes green. Task 2 watchpoint: identity closure must derive or reject module globals rather than trust manual dependency tuples. |
| 2. Immutable production registrations and transitive identity | completed | `0afe4b1` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Behavioral missing-module RED, class-method-helper RED/GREEN, focused identity GREEN, and 110-test Task 2 subsystem GREEN recorded in `task-2-report.md`. Single 39-record platform registry, deep immutable views, exact dependency/body closure, logic kernels, all generated blocks, complete expanding-z registrations, and real `_rma` stale-address proof. Compile/diff/protected hashes green. |
| 3. Immutable admission artefact and refusal codes | completed | `d37ac30` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Missing-module RED, self-referential graph RecursionError RED/typed-refusal GREEN, focused admission GREEN, and exact 75-test Task 3 subsystem GREEN recorded in `task-3-report.md`. Structural inspection binds owner, reached nested body refs, derived provenance, exact sockets/contracts/history/mapping/risk, fresh implementation identity, and handwritten executable identity. Compile/diff/protected hashes green. |
| 4. Independent prefix reference, parity, and mutations | completed | `e69d9c1` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Exact scoped Task 4 suite and full admission tests exited 0; shipped admission returned verified `IRGraphStrategy`; its 302 pre-warmup bars emitted zero canonical signals; forged receipt, deterministic state replay, 23 admitted manifest blocks, 32 RSI choices, session/regime transitions, stale identity, and five fresh causal mutants passed their named gates. Protected hashes, compilation, and diff checks are green. Full evidence is recorded in `task-4-report.md`. |
| 5. Mechanical Composition-to-IR lowering | completed | `0ac276c` | **Critical:** independent review SPEC PASS / QUALITY PASS. Mechanical lowering, app-registry-only generated authority, durable graph/receipt descriptors, and pre-provider forged graph/receipt refusal passed their targeted gates. `test_composition_ir.py` reports 10 passed; the legacy-authority bypass, durable descriptor round-trip, operation schema, compilation, diff, staged, and protected-hash checks are green. Task 8 owns persisted-receipt verification for the separate Gen2 owner-scoped graph worker. See `task-5-report.md`. |
| 6. Execution-plane persistence and migration 0034 | completed | `0b82cfc` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Owner scope, canonical identity, exact duplicate/conflict behavior, nullable checked consumer references, USER-plane classification, migration parity, and full PostgreSQL trigger contract passed. Live PostgreSQL 16 raw UPDATE and DELETE both returned SQLSTATE `55000` and retained byte-identical receipts. See `task-6-report.md`. |
| 7. Research-plane persistence and migration 0005 | completed | `f2fdcd9` | **Critical:** independent final review SPEC PASS / QUALITY PASS. SQLite/recovery boundary is 39 passed, 1 skipped and live PostgreSQL 16 is 15 passed. Evidence covers canonical bytes, owner/run/candidate binding, direct SQL immutability, exact 0005/head semantics, inert trigger-catalog refusal, strict historical preflight, and real 0004 legacy-null preservation. See `task-7-report.md`. |
| 8. Editor publication and research enforcement | completed | `6c3e2b4` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Editor refusal preserves the valid edit as an unpublished draft; accepted publication binds receipt and version atomically. Graph and generated research workers reload and freshly verify owner-local receipts before provider/data I/O. Durable failure event and outbox evidence retain the stable refusal code. Fix round 1 moved typed handling to the direct version-publication route and proved no version, pointer, or receipt advances on refusal. See `task-8-report.md`. |
| 9. Backtest admission, cache identity, and next-bar execution | completed | `0c784d3` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Enqueue, post-reservation resolution, worker, and reclaim freshly verify exact owner-local receipt/graph bytes against current registry before provider/data access; one receipt cannot cover a multi-strategy run; every result address exactly equals its fenced parent run; run/result/cache identity bind the receipt; enqueue, worker, and same-bar mutants are killed. Fix-round boundary is 36 passed; independent re-review is 28 passed. Public payloads remain neutral and cannot cross an execution-address receipt boundary. See `task-9-report.md`. |
| 10. Promotion, shadow, paper, and deployment authority | completed | `552488c` | **Critical:** independent final review SPEC PASS / QUALITY PASS. Promotion approval freshly verifies exact candidate, run, receipt, graph identifier, version, and bytes. Shadow and paper activate, resume, and reload require the owner-local receipt; reload does not query research. Nonlegacy deployment writes and deploy-bridge requests require a current receipt plus an exact owner-owned active account. All seven named authority mutants are killed individually. Focused compatibility checks cover admitted deployment creation and bridge happy, forged-receipt, and foreign-account paths. See `task-10-report.md`. |
| 11. Execution attribution and legacy quarantine | completed | pending scoped commit | **Critical:** independent final review SPEC PASS / QUALITY PASS. Freshly verified admitted IR produces every new-entry decision and the final order boundary rechecks authority. Broker-owned entry seams reject forged receipts; post-submit and late fills book from the exact persisted intent without reopening authority. Intent, position, and trade retain one immutable receipt. Legacy exits remain open while new legacy exposure is refused. Additive backfill writes only exact receipts, quarantines missing historical facts, and preserves immutable graph rows. Root retained 38 binding/attribution passes plus the corrected null-GraphVersion fallback probe; focused backfill and mutation gates are recorded in `task-11-report.md`. |
| 12. Mutation gate and Phase 3 closure | pending | — | — |
